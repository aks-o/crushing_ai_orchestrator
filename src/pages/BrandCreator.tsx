importimport React, { useState } fromimport React, { useState } from 'react';
import { useimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Buildingimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulbimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Checkimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-reactimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } fromimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
importimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../storeimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
importimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } fromimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../libimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintechimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'Saaimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturingimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Foodimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachableimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Simport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

exportimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } =import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStepimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(falseimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData,import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useStateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName:import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] asimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience:import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrorsimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<stringimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<stringimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formDataimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company nameimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      }import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyNameimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 charactersimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      ifimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Pleaseimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    ifimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2)import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5)import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personalityimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    ifimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3)import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formDataimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      }import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience =import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at leastimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Objectimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  constimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    ifimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      ifimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    ifimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(trueimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      constimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAIimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrorsimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' }import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality =import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => (import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includesimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? previmport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !==import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...previmport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bgimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bgimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="pimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 maximport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="fleximport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div classNameimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br fromimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 himport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 classimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl fontimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 darkimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create Newimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="textimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AIimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for youimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="fleximport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1,import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].mapimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  classNameimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'wimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-fullimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-smimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    stepimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bgimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ?import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-whiteimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-simport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > simport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="wimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" />import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  classimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:blockimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      :import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 &&import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s ===import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hiddenimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bgimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="spaceimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label classNameimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium textimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-whiteimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Buildingimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <inputimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyNameimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(eimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: eimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholderimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  classimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white darkimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-simport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholderimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errorsimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'borderimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:borderimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blueimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <pimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 textimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </divimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="blockimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers classimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 himport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div classimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industryimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <buttonimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industryimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg borderimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all durationimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formDataimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ?import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bgimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blueimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 darkimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          :import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slate-950 border-slate-300 darkimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slate-950 border-slate-300 dark:border-slate-700 text-simport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slate-950 border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slateimport React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slate-950 border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:border-blue-400import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slate-950 border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:border-blue-400 hover:text-blue-600'
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slate-950 border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:border-blue-400 hover:text-blue-600'
                      )}
                    >
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Sparkles, Building2, Users, Lightbulb, ArrowRight, Loader2, Check } from 'lucide-react';
import { Button } from '../components/ui/Button';
import { useBrandStore } from '../store/brandStore';
import { generateBrandWithAI } from '../lib/aiGeneration';
import { cn } from '../lib/utils';

const industryOptions = [
  'Fintech',
  'E-commerce',
  'Healthcare',
  'Education',
  'SaaS',
  'AI/ML',
  'Manufacturing',
  'Real Estate',
  'Consulting',
  'Non-profit',
  'Food & Beverage',
  'Entertainment',
];

const personalityOptions = [
  'Professional',
  'Friendly',
  'Innovative',
  'Trustworthy',
  'Playful',
  'Luxurious',
  'Minimalist',
  'Bold',
  'Approachable',
  'Technical',
  'Creative',
  'Sustainable',
];

export default function BrandCreator() {
  const navigate = useNavigate();
  const { addBrand } = useBrandStore();
  
  const [step, setStep] = useState(1);
  const [isGenerating, setIsGenerating] = useState(false);
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    personality: [] as string[],
    audience: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const validateStep = (currentStep: number): boolean => {
    const newErrors: Record<string, string> = {};
    
    if (currentStep === 1) {
      if (!formData.companyName.trim()) {
        newErrors.companyName = 'Company name is required';
      } else if (formData.companyName.length < 2) {
        newErrors.companyName = 'Name must be at least 2 characters';
      }
      if (!formData.industry) {
        newErrors.industry = 'Please select an industry';
      }
    }
    
    if (currentStep === 2) {
      if (formData.personality.length === 0) {
        newErrors.personality = 'Select at least one personality trait';
      }
      if (formData.personality.length > 5) {
        newErrors.personality = 'Select up to 5 personality traits';
      }
    }
    
    if (currentStep === 3) {
      if (!formData.audience.trim()) {
        newErrors.audience = 'Describe your target audience';
      } else if (formData.audience.length < 10) {
        newErrors.audience = 'Please provide more detail (at least 10 characters)';
      }
    }
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleNext = () => {
    if (validateStep(step)) {
      if (step < 3) {
        setStep(step + 1);
      } else {
        handleGenerate();
      }
    }
  };

  const handleGenerate = async () => {
    if (!validateStep(step)) return;
    
    setIsGenerating(true);
    try {
      const newBrand = await generateBrandWithAI(formData);
      addBrand(newBrand);
      navigate(`/brand/${newBrand.id}/identity`);
    } catch (error) {
      console.error('Failed to generate brand:', error);
      setErrors({ submit: 'Failed to generate brand. Please try again.' });
    } finally {
      setIsGenerating(false);
    }
  };

  const togglePersonality = (trait: string) => {
    setFormData(prev => ({
      ...prev,
      personality: prev.personality.includes(trait)
        ? prev.personality.filter(p => p !== trait)
        : [...prev.personality, trait],
    }));
  };

  return (
    <div className="min-h-screen bg-slate-50 dark:bg-slate-950 pt-16">
      <div className="p-6 lg:p-8 max-w-4xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <Sparkles className="w-6 h-6 text-white" />
            </div>
            <div>
              <h1 className="text-2xl font-bold text-slate-900 dark:text-white">
                Create New Brand
              </h1>
              <p className="text-slate-600 dark:text-slate-400">
                Let AI generate a complete brand system for you
              </p>
            </div>
          </div>

          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-6">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center gap-3">
                <div
                  className={cn(
                    'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium transition-colors',
                    step === s
                      ? 'bg-blue-600 text-white'
                      : step > s
                      ? 'bg-green-500 text-white'
                      : 'bg-slate-200 dark:bg-slate-800 text-slate-500'
                  )}
                >
                  {step > s ? <Check className="w-4 h-4" /> : s}
                </div>
                <span
                  className={cn(
                    'text-sm font-medium hidden sm:block',
                    step === s
                      ? 'text-slate-900 dark:text-white'
                      : 'text-slate-500'
                  )}
                >
                  {s === 1 && 'Basic Info'}
                  {s === 2 && 'Personality'}
                  {s === 3 && 'Audience'}
                </span>
                {s < 3 && (
                  <div className="hidden sm:block w-12 h-0.5 bg-slate-200 dark:bg-slate-800" />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Form Content */}
        <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm p-6 lg:p-8">
          {step === 1 && (
            <div className="space-y-6">
              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Building2 className="inline w-4 h-4 mr-2" />
                  Company Name *
                </label>
                <input
                  type="text"
                  value={formData.companyName}
                  onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
                  placeholder="Enter your company name"
                  className={cn(
                    'w-full px-4 py-3 rounded-lg border bg-white dark:bg-slate-950 text-slate-900 dark:text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 transition-colors',
                    errors.companyName
                      ? 'border-red-500 focus:border-red-500'
                      : 'border-slate-300 dark:border-slate-700 focus:border-blue-500'
                  )}
                />
                {errors.companyName && (
                  <p className="mt-1 text-sm text-red-500">{errors.companyName}</p>
                )}
              </div>

              <div>
                <label className="block text-sm font-medium text-slate-900 dark:text-white mb-2">
                  <Layers className="inline w-4 h-4 mr-2" />
                  Industry Vertical *
                </label>
                <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                  {industryOptions.map((industry) => (
                    <button
                      key={industry}
                      onClick={() => setFormData({ ...formData, industry })}
                      className={cn(
                        'px-4 py-3 rounded-lg border text-sm font-medium transition-all duration-200',
                        formData.industry === industry
                          ? 'bg-blue-50 dark:bg-blue-900/20 border-blue-500 text-blue-700 dark:text-blue-400'
                          : 'bg-white dark:bg-slate-950 border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:border-blue-400 hover:text-blue-600'
                      )}
                    >
                      {industry}
                    </